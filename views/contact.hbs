<!DOCTYPE html>
<html lang="en" dir="ltr">
  <h2>Contact me</h2>
  <form action="/contact/send-message" method="POST" enctype="multipart/form-data">
    {{#if isSent}}
    <div class="isSent">The message and file {{ imageName }} has been sent</div>
    {{/if}}
    {{#if isError}}
    <div class="isError">You can't leave fields empty!</div>
    {{/if}}
    {{#if isErrorImage}}
    <div class="isError">You have not included file or you have chosen wrong file extension !</div>
    {{/if}}
    <label>
      Your name: <input type="text" name="author">
    </label>
    <label>
      Your email: <input type="email" name="sender">
    </label>
    <label>
      Title: <input type="text" name="title">
    </label>
    <label>
      Project design: <input type="file" name="image" accept="image/jpeg, image/jpg, image/png, image/gif">
    </label>
    <label>
      Message: <textarea name="message"></textarea>
    </label>
    <button type="submit">Send message</button>
  </form>
</html>